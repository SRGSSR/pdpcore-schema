@namespace("ch.srgssr.pdp.domain.egest")
protocol Provenance {

    /** The source system publishing the metadata. */
    enum Provenance {
        Archive,
        CMS,
        Play,
        PlaySuisse,
        OfferPortfolio,
        AIS,
        Digas,
        OpenMedia,
        MOVEit,
        WhatsOn,
        Unknown
    } = Unknown;

}

@namespace("ch.srgssr.pdp.domain.egest")
protocol Essence {

    /** A reference to the playable asset. */
    record Essence {
        /** The url. */
        union{null, string} locator = null;
    }

}

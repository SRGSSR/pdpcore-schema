@namespace("ch.srgssr.pdp.domain.egest")
protocol Rights {

    record Rights {
        /** An agent (contact, person or organisation) having or managing the rights to an asset. */
        union{null, string} rightsHolder;
        /** Expresses usage rights. */
        array<string> usageRights = [];
    }

}

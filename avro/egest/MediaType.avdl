@namespace("ch.srgssr.pdp.domain.egest")
protocol MediaType {

    /** Used to further specify the audiovisual asset type. */
    enum MediaType {
        Broadcast, Movie, Series, Podcast, LiveStream, Trailer, Unknown
    } = Unknown;

}

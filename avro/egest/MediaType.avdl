@namespace("ch.srgssr.pdp.domain.ingest")
protocol MediaType {

    /** Used to further specify the audiovisual asset type. */
    enum MediaType {
        Broadcast, Movie, Serie, Podcast, Livestream, Trailer, Unknown
    }

}